package com.ib.urireg.quartz;

import com.ib.system.quartz.BaseJobListener;

public class Sent2OpenDataJobListener extends BaseJobListener {
    /**
     * @param listenerName
     */
    public Sent2OpenDataJobListener(String listenerName) {
        super(listenerName);
    }
}
