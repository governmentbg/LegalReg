package com.ib.urireg.quartz;

import com.ib.system.quartz.BaseJobListener;

public class IndexFileContentJobListener extends BaseJobListener {
    /**
     * @param listenerName
     */
    public IndexFileContentJobListener(String listenerName) {
        super(listenerName);
    }
}
